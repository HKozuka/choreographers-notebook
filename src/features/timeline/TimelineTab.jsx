export default function TimelineTab({ projectId }) {
  return (
    <div style={{ padding: 'var(--space-lg) 0', color: 'var(--color-text-secondary)', fontStyle: 'italic', fontFamily: 'var(--font-display)' }}>
      Visualization Timeline — coming in Sub-Task 9
    </div>
  )
}
